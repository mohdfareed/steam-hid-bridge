<Window x:Class="SteamHidBridge.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:SteamHidBridge.App.Views"
        mc:Ignorable="d"
        Title="Steam HID Bridge"
        Width="980"
        Height="620"
        MinWidth="860"
        MinHeight="520"
        Background="#F6F7F9"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="BorderBrush" Value="#D7DCE2" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1F2933" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Steam HID Bridge" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="{Binding ProfileText}" Margin="0,5,0,0" Foreground="#5A6878" />
            </StackPanel>
            <Border DockPanel.Dock="Right"
                    Padding="12,7"
                    BorderBrush="#B8C4D0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Background="#FFFFFF"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10"
                             Height="10"
                             Fill="{Binding StatusBrush}"
                             Margin="0,0,8,0"
                             VerticalAlignment="Center" />
                    <TextBlock Text="{Binding ForwardingStatus}" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <views:GameSettingsView />
            <views:OutputView Grid.Column="2" />
        </Grid>
    </Grid>
</Window>
