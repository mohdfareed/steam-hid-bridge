<UserControl x:Class="SteamHidBridge.App.Ui.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
        <Grid MinWidth="640">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Process" />
            <TextBox Grid.Column="1"
                     Text="{Binding ProcessText, Mode=OneWay}"
                     IsReadOnly="True" />

            <TextBlock Grid.Row="1" Text="Forwarding" Margin="0,8,0,0" />
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Text="{Binding ForwardingStatus, Mode=OneWay}"
                     IsReadOnly="True"
                     Margin="0,8,0,0" />

            <TextBlock Grid.Row="2" Text="Activity" Margin="0,8,0,0" />
            <TextBox Grid.Row="2"
                     Grid.Column="1"
                     Text="{Binding ActivityText, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     Margin="0,8,0,0" />

            <TextBlock Grid.Row="3" Text="Input" Margin="0,8,0,0" />
            <TextBox Grid.Row="3"
                     Grid.Column="1"
                     Text="{Binding InputSourceText, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     Margin="0,8,0,0" />

            <TextBlock Grid.Row="4" Text="Output" Margin="0,8,0,0" />
            <TextBox Grid.Row="4"
                     Grid.Column="1"
                     Text="{Binding OutputTargetText, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     Margin="0,8,0,0" />

            <TextBlock Grid.Row="5" Text="App data" Margin="0,8,0,0" />
            <TextBox Grid.Row="5"
                     Grid.Column="1"
                     Text="{Binding AppDataPath, Mode=OneWay}"
                     IsReadOnly="True"
                     Margin="0,8,0,0" />
        </Grid>
    </ScrollViewer>
</UserControl>
